CRUX portfolio preview: actual cached RGB faces, current hold segmentation and validated generated route. This is a staged visualization, not a real-time performance recording. Regenerate with Python + Pillow and ffmpeg on PATH: python scripts/crux-preview/render.py. The source photo is public/crux/examples/overhang.png; outputs go to public/assets/projects/crux/. The Windows Arial paths can be replaced with an available equivalent font.
